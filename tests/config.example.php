<?php
/**
 * Конфигурация функциональных тестов PHPMaxBot
 *
 * 1. Скопируйте: cp tests/config.example.php tests/config.php
 * 2. Заполните значения ниже
 * 3. Убедитесь, что tests/config.php добавлен в .gitignore
 *
 * Как узнать TEST_CHAT_ID и TEST_USER_ID:
 *   Запустите php tests/updates.php, напишите в чат с ботом — ID появится в выводе.
 */

// Токен бота (получить у @MaxBotFather в MAX)
define('BOT_TOKEN', 'your-bot-token-here');

// ID группового чата, в котором бот является участником
// Если 0 — тесты, требующие группового чата, будут пропущены
define('TEST_CHAT_ID', 0);

// ID пользователя для тестов личных сообщений (опционально)
// Пользователь должен хотя бы раз написать боту (/start)
// Если 0 — тесты с личными сообщениями будут пропущены
define('TEST_USER_ID', 0);
